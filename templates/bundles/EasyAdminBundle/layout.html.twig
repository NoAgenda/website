{% extends '@!EasyAdmin/layout.html.twig' %}

{% block page_title -%}
    {%- set title = block('content_title') -%}
    {%- set suffix = ea.dashboardTitle|raw -%}
    {{ title != suffix ? "#{title} - #{suffix}" : suffix }}
{%- endblock %}
