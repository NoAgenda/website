{% set page_title = page_title|default('No Agenda Show') %}
{% set body = block('body') %}
{% set metatags = block('metatags') %}
{% set request_path = ajaxRequestPath() %}

{% set output = {
  'path': request_path,
  'fragment': app.request.query.get('_fragment'),
  'title': page_title,
  'meta': metatags,
  'contents': body,
  'authenticated': authenticated,
} %}

{{ output|json_encode|raw }}
