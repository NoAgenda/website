{% set page_title = page_title is not defined ? 'No Agenda Experience' : "#{page_title} - No Agenda Experience" %}
{% set body = block('body') %}

{% set output = {
  'title': page_title,
  'contents': body,
  'authenticated': authenticated,
} %}

{{ output|json_encode|raw }}
